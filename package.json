{
  "name": "collective-unconscious",
  "version": "1.0.0",
  "description": "Vectorized fleet memory in Cloudflare — the collected unconscious, stamped by time",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "ingest": "tsx scripts/ingest.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240901.0",
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^3.80.0"
  }
}
